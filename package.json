{
  "name": "smart-scraper",
  "version": "1.0.0",
  "description": "A web scraper for Amazon India smart TV listings",
  "main": "index.js",
  "scripts": {
    "start": "npm run start:server",
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "start:server": "cd server && npm start",
    "build": "cd client && npm install && npm run build",
    "postbuild": "cp -r client/build server/public || true",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "heroku-postbuild": "npm run install:all && npm run build",
    "render-postbuild": "cd client && npm install --production=false && npm run build"
  },
  "keywords": ["web-scraper", "amazon", "smart-tv", "puppeteer"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 